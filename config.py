from modes import Modes, Versions, WordSelModes

class config:
    PREDICT = False
    MODE = Modes.blatant
    GG = ""
    getWordLimit = 25
    maxWordLength = 25
    autoType = True
    sleepTime = 0.05
    VERSION = Versions.Korea
    wordSelMode = WordSelModes.random

Config = config()